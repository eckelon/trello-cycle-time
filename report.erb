<!doctype html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Average Cycle for <%= board_name %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/foundation/6.2.4/foundation.min.css">
    <style type="text/css">
      .content {
        margin: 1rem 0;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <div class="row">
        <div class="callout large primary">
          <div class="row column text-center">
            <h1>Average cycle time is: <strong><%= (average_cycle_time/(3600 * 24)).round(2) %></strong> days</h1>
          </div>
        </div>
      </div>

      <div class="row">
        <table class="hover">
          <thead>
            <tr>
              <th/>
              <th width="55%">Card</th>
              <th>Started on</th>
              <th>Finished on</th>
              <th width="10%">Cycle time</th>
            </tr>
          </thead>
          <tbody>
            <% cards.each do |card|%>
              <tr>
                <td><a href="<%= card.url%>"><%= card.short_id %></a></td>
                <td><a href="<%= card.url%>"><%= card.name %></a></td>
                <td><%= card.started_on.localtime.strftime("%d-%m-%Y %H:%M") %></td>
                <td><%= card.finished_on.localtime.strftime("%d-%m-%Y %H:%M") %></td>
                <td><%= (card.cycle_time / 3600).round(2) %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
